Перед запуском в production-среде необходимо заполнить .env файл.
При локальном запуске можно оставить .env файл без изменений.

Запуск:
$ docker-compose build
$ docker-compose up

Джанго-админка будет доступна по следующему адресу:
http://127.0.0.1:80/admin/

Для сборки статики выполнить:
$ make static

Для проведения миграции:
$ make migration